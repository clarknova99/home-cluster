---
# install python pre-reqs
# apt update; apt install build-essential libssl-dev zlib1g-dev \
# libbz2-dev libreadline-dev libsqlite3-dev curl llvm \
# libncursesw5-dev xz-utils tk-dev libxml2-dev libxmlsec1-dev libffi-dev liblzma-dev

#pip install ansible proxmoxer requests
all:
  children:
    proxmox:
      hosts:
        nuc:
          ansible_host: 192.168.1.9
        nuc11:
          ansible_host: 192.168.1.7
      vars:
        ansible_user: root
        ansible_python_interpreter: /root/.pyenv/versions/3.11.1/bin/python3
        hostname: lxc_hostname


