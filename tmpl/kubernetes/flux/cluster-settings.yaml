---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-settings
  namespace: flux-system
data:
  TIMEZONE: "${BOOTSTRAP_TIMEZONE}"
  KUBE_VIP_ADDR: "${BOOTSTRAP_KUBE_VIP_ADDR}"
  METALLB_LB_RANGE: "${BOOTSTRAP_METALLB_LB_RANGE}"
  METALLB_K8S_GATEWAY_ADDR: "${BOOTSTRAP_METALLB_K8S_GATEWAY_ADDR}"
  METALLB_INGRESS_ADDR: "${BOOTSTRAP_METALLB_INGRESS_ADDR}"
  TRAEFIK_SERVICE_ADDR: "${BOOTSTRAP_TRAEFIK_INGRESS_ADDR}"
  NFS_SERVER_ADDR: "${BOOTSTRAP_NFS_SERVER_ADDR}"
  NFS_SERVER_PATH: "${BOOTSTRAP_NFS_SERVER_PATH}"
  PROXMOX_ADDR: "${BOOTSTRAP_PROXMOX_ADDR}"
  PLEX_ADDR: "${BOOTSTRAP_PLEX_ADDR}"
