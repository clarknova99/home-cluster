---
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: "${BOOTSTRAP_CLOUDFLARE_DOMAIN}"
  SECRET_CLOUDFLARE_EMAIL: "${BOOTSTRAP_CLOUDFLARE_EMAIL}"
  SECRET_GOOGLE_CLIENT_ID: "${BOOTSTRAP_GOOGLE_CLIENT_ID}"
  SECRET_GOOGLE_CLIENT_SECRET: "${BOOTSTRAP_GOOGLE_CLIENT_SECRET}"
  SECRET_GOOGLE_WHITELIST_EMAIL: "${BOOTSTRAP_GOOGLE_WHITELIST_EMAIL}"
  SECRET_AUTHELIA_EMAIL: "${BOOTSTRAP_AUTHELIA_EMAIL}"
  SECRET_AUTHELIA_DISPLAY_NAME: "${BOOTSTRAP_AUTHELIA_DISPLAY_NAME}"
  SECRET_AUTHELIA_PASSWORD: "${BOOTSTRAP_AUTHELIA_PASSWORD}"
  SECRET_AUTHELIA_JWT: "${BOOTSTRAP_AUTHELIA_JWT}"
  SECRET_LLDAP_JWT: "${BOOTSTRAP_LLDAP_JWT}"
  SECRET_LLDAP_LDAP_USER_PASS: "${BOOTSTRAP_LLDAP_LDAP_USER_PASS}"
  SECRET_LLDAP_LDAP_BASE_DN: "${BOOTSTRAP_LLDAP_LDAP_BASE_DN}"
  SECRET_AUTHENTIK_KEY: ${BOOTSTRAP_AUTHENTIK_KEY}
  SECRET_AUTHENTIK_SMTP_HOST: ${BOOTSTRAP_AUTHENTIK_SMTP_HOST}
  SECRET_AUTHENTIK_SMTP_USERNAME: ${BOOTSTRAP_AUTHENTIK_SMTP_USERNAME}
  SECRET_AUTHENTIK_SMTP_PASSWORD: ${BOOTSTRAP_AUTHENTIK_SMTP_PASSWORD}
  SECRET_AUTHENTIK_POSTGRES_HOST: ${BOOTSTRAP_AUTHENTIK_POSTGRES_HOST}
  SECRET_AUTHENTIK_POSTGRES_USERNAME: ${BOOTSTRAP_AUTHENTIK_POSTGRES_USERNAME}
  SECRET_AUTHENTIK_POSTGRES_PASSWORD: ${BOOTSTRAP_AUTHENTIK_POSTGRES_PASSWORD}
  SECRET_POSTGRES_USERNAME: ${BOOTSTRAP_POSTGRES_USERNAME}
  SECRET_POSTGRES_PASSWORD: ${BOOTSTRAP_POSTGRES_PASSWORD}
