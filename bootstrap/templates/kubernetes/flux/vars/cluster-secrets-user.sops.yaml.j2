---
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets-user
  namespace: flux-system
stringData:
  SECRET_PLACEHOLDER: "secret-value"
  SECRET_MAXMIND_LICENSE_KEY: "{{ bootstrap_maxmind_license_key }}"
  SECRET_POSTGRES_USERNAME: "{{ bootstrap_postgres_username }}"
  SECRET_POSTGRES_PASSWORD: "{{ bootstrap_postgres_password }}"
