apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: cloudnative-pg-cluster-backup
  namespace: database
spec:
  cluster:
    name: cloudnative-pg-cluster
# kubectl -f kubernetes/apps/database/cloudnative-pg/cluster/backup.yaml apply
