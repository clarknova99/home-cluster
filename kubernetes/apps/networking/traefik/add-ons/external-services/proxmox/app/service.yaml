apiVersion: v1
kind: Service
metadata:
  name: traefik-add-ons-external-services-proxmox
spec:
  ports:
    - name: https
      protocol: TCP
      port: 8006
      targetPort: https
  type: ClusterIP
