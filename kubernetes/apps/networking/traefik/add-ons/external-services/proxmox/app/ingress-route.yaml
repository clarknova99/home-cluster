---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-add-ons-external-services-proxmox
  namespace: networking
  labels:
    app.kubernetes.io/name: proxmox
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - kind: Rule
      match: Host(`proxmox.${SECRET_DOMAIN}`)
      services:
        - name: traefik-add-ons-external-services-proxmox
          port: 8006
          #namespace: networking
          serversTransport: traefik-add-ons-external-services-proxmox
      middlewares:
        - name: traefik-middleware-chains-basic-auth
          namespace: networking
  # tls:
  #   - secretName:
  #     - "cluster-secrets"
  #     - "proxmox-${SECRET_DOMAIN/./-}-tls"
  #   #secretName: "proxmox-${SECRET_DOMAIN/./-}-tls"
  #   #secretName: "${SECRET_DOMAIN/./-}-tls"
  #   #secretName: wildcard-tls
  #   # secretName: "proxmox-${SECRET_DOMAIN/./-}-tls"

---
apiVersion: traefik.containo.us/v1alpha1
kind: ServersTransport
metadata:
  name: traefik-add-ons-external-services-proxmox
  labels:
    app.kubernetes.io/name: traefik-add-ons-external-services-proxmox
spec:
  insecureSkipVerify: true
