---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-signoz
  namespace: monitoring
spec:
  storageClassName: "network-storage"
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: 192.168.1.2
    path: "/volume1/network-storage/signoz"
    readOnly: false
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-signoz
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteMany
  volumeName: pv-signoz
  storageClassName: "network-storage"
  resources:
    requests:
      storage: 1Gi
