---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: retool
  namespace: default
spec:
  interval: 30m
  chart:
    spec:
      chart: appsmith
      version: 2.0.3
      sourceRef:
        kind: HelmRepository
        name: retool
        namespace: flux-system
  values:
