---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: echo
  labels:
    app: echo-server
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`echo-server.${SECRET_DOMAIN}`)
      kind: Rule
      services:
        - name: echo-server
          port: 8080
  # tls:
  #   secretName: "echo.${SECRET_DOMAIN}-prod-cert"
