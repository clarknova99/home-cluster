---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whoami-test
  namespace: default
  annotations:
    nfs.io/storage-path: "test"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-provisioner
