apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test
  labels:
    app.kubernetes.io/name: storage
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: nfs-provisioner

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: testing
  labels:
    app.kubernetes.io/name: storage
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: nfs-provisioner
